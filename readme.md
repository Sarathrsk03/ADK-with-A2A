To run the program

Current Directory must be the parent directory

1. Open two terminals 
2. In the first terminal run uvicorn agent:a2a_app --host localhost --port 8001
3. In the second terminal run adk web